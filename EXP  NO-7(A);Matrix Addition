AIM: 
To write a C program to perform the addition of two matrices. 
 
ALGORITHM: 
1. Start the program. 
2. Declare variables for two matrices and the sum matrix. 
3. Read the number of rows and columns. 
4. Read elements for the first matrix. 
5. Read elements for the second matrix. 
6. Perform addition: 
  -> sum[i][j] = mat1[i][j] + mat2[i][j] 
7. Display the sum matrix. 
8. Stop. 
 

PROGRAM:  
#include <stdio.h> 
int main() { 
int mat1[10][10], mat2[10][10], sum[10][10]; 
int r, c, i, j; 
printf("Enter number of rows and columns: "); 
scanf("%d %d", &r, &c); 
printf("Enter elements of first matrix:\n"); 
for (i = 0; i < r; i++) { 
for (j = 0; j < c; j++) { 
scanf("%d", &mat1[i][j]); 
} 
} 
printf("Enter elements of second matrix:\n"); 
for (i = 0; i < r; i++) { 
for (j = 0; j < c; j++) { 
scanf("%d", &mat2[i][j]); 
} 
} 
// Matrix Addition 
for (i = 0; i < r; i++) { 
for (j = 0; j < c; j++) { 
sum[i][j] = mat1[i][j] + mat2[i][j]; 
} 
} 
printf("\nSum of matrices:\n"); 
for (i = 0; i < r; i++) { 
for (j = 0; j < c; j++) { 
printf("%d\t", sum[i][j]); 
} 
printf("\n"); 
} 
return 0; 
} 
 
 
SAMPLE OUTPUT 
Enter number of rows and columns: 2 2 
Enter elements of first matrix: 
1 2 
3 4 
Enter elements of second matrix: 
5 6 
7 8 
Sum of matrices: 
6  8 
10 12
